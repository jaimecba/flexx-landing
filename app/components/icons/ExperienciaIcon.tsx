import React from "react";

const ExperienciaIcon: React.FC<{ className?: string }> = ({ className }) => (
    <img src="/icons/experiencia.png" alt="Ícone de experiência" className={className} />
);

export default ExperienciaIcon;