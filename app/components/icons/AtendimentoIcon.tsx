import React from "react";

const AtendimentoIcon: React.FC<{ className?: string }> = ({ className }) => (
    <img src="/icons/atendimento.png" alt="Ícone de atendimento" className={className} />
);

export default AtendimentoIcon;