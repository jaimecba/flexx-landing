import React from "react";

const RecomendacaoIcon: React.FC<{ className?: string }> = ({ className }) => (
    <img src="/icons/recomendacao.png" alt="Ícone de recomendação" className={className} />
);

export default RecomendacaoIcon;